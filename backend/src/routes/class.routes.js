import express from 'express';import {  getClasses,  getClassById,  createClass,  updateClass,  deleteClass,  addStudentsToClass,  removeStudentFromClass,  removeStudentsFromClass,  getClassAnalysis,  getStudentDetails,} from '../controllers/class.controller.js';import { protect, authorize } from '../middleware/auth.middleware.js';import { body } from 'express-validator';const router = express.Router();router.use(protect);router.use(authorize('teacher'));const validateClass = [  body('name').trim().notEmpty().withMessage('Class name is required'),  body('description').optional().trim(),  body('teacherName').optional().trim(),  body('institution').optional().trim(),];router.get('/', getClasses);router.get('/:id', getClassById);router.post('/', validateClass, createClass);router.put('/:id', validateClass, updateClass);router.delete('/:id', deleteClass);router.post('/:id/students', addStudentsToClass);router.delete('/:id/students/:studentId', removeStudentFromClass);router.delete('/:id/students', removeStudentsFromClass);router.get('/:id/analysis', getClassAnalysis);router.get('/:classId/students/:studentId', getStudentDetails);export default router;