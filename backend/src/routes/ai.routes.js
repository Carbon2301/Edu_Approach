import express from 'express';import {  generateRecommendations,  getRecommendations,  checkRecommendationStatus,} from '../controllers/ai.controller.js';import { generateSingleQuizRecommendations } from '../controllers/ai-single.controller.js';import { generateTeachingRecommendations, generateClassTeachingRecommendations } from '../controllers/ai-teaching.controller.js';import { protect } from '../middleware/auth.middleware.js';const router = express.Router();router.use(protect);router.post('/recommendations', generateRecommendations);router.get('/recommendations', getRecommendations);router.get('/recommendations/status', checkRecommendationStatus);router.post('/recommendations/single/:resultId', generateSingleQuizRecommendations);router.post('/teaching-recommendations/class/:classId', generateClassTeachingRecommendations);router.post('/teaching-recommendations/:classId/:studentId', generateTeachingRecommendations);export default router;