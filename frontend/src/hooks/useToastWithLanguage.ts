import { useToast } from './useToast';import { useLanguage } from '../contexts/LanguageContext';export const useToastWithLanguage = () => {  const toast = useToast();  const { t } = useLanguage();  return {    ...toast,    success: (key: string, customMessage?: string, duration?: number) => {      return toast.success(customMessage || t(key), duration);    },    error: (key: string, customMessage?: string, duration?: number) => {      return toast.error(customMessage || t(key), duration);    },    warning: (key: string, customMessage?: string, duration?: number) => {      return toast.warning(customMessage || t(key), duration);    },    info: (key: string, customMessage?: string, duration?: number) => {      return toast.info(customMessage || t(key), duration);    },  };};