import { User } from 'lucide-react';import { useNavigate } from 'react-router-dom';import { useLanguage } from '../contexts/LanguageContext';interface UserProfileCardProps {  user: any;}const UserProfileCard = ({ user }: UserProfileCardProps) => {  const navigate = useNavigate();  const handleClick = () => {    if (user?.role === 'teacher') {      navigate('/teacher/profile');    } else {      navigate('/profile');    }  };  return (    <div       className="fixed bottom-0 left-0 z-40 cursor-pointer hover:shadow-2xl transition-all hidden sm:block"      onClick={handleClick}    >      <div className="bg-white shadow-xl border-t-2 border-r-2 border-gray-900 rounded-tr-xl">        <div className="flex items-center gap-2 p-2 sm:p-2.5 lg:p-3">          {user?.profileImage ? (            <img              src={user.profileImage}              alt="Profile"              className="w-10 h-10 sm:w-11 sm:h-11 lg:w-12 lg:h-12 rounded-full object-cover border-2 border-gray-900 flex-shrink-0"            />          ) : (            <div className="w-10 h-10 sm:w-11 sm:h-11 lg:w-12 lg:h-12 rounded-full bg-gray-200 flex items-center justify-center border-2 border-gray-900 flex-shrink-0">              <User size={20} className="text-gray-400 sm:w-5 sm:h-5 lg:w-6 lg:h-6" />            </div>          )}          <div className="flex-1 min-w-0 pr-2 sm:pr-2.5 lg:pr-3 max-w-[160px] sm:max-w-[180px] lg:max-w-[200px]">            <p className="font-bold text-gray-900 text-xs sm:text-sm truncate leading-tight">              {user?.name}            </p>            <p className="text-[10px] sm:text-xs text-gray-600 truncate leading-tight">              {user?.email}            </p>          </div>        </div>      </div>    </div>  );};export default UserProfileCard;